{
  "name": "commercialexperts-e2e",
  "version": "1.0.0",
  "description": "Playwright E2E tests for CommercialExperts.com application journeys",
  "scripts": {
    "test": "playwright test",
    "test:cwuk": "playwright test --grep @cwuk",
    "test:fcus": "playwright test --grep @fcus",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "typescript": "^5.4.0"
  }
}
